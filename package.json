{
  "name": "atlas-auth-agent",
  "version": "1.0.0",
  "description": "Agente autónomo para generar sistemas de autenticación",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --workspace=harness",
    "agent": "node agent.js",
    "test": "node --test test/**/*.test.js"
  },
  "workspaces": ["harness"],
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "fs-extra": "^11.2.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}